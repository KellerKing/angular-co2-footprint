import { Routes } from '@angular/router';
import { PageTabelle } from './page-tabelle/page-tabelle';

export const routes: Routes = [
    {
        path: 'tabelle',
        component: PageTabelle
    }
];
